# ADX sink extra — the one sanctioned dependency exception, isolated so a benchmark run
# works with requirements.txt alone. Imported lazily by sinks/adx.py.
-r requirements.txt
azure-kusto-data>=4.5.0
azure-kusto-ingest>=4.5.0
azure-identity>=1.17.0
